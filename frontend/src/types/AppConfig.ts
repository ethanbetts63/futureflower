// foreverflower/frontend/src/types/config.ts

/**
 * Defines the structure for application configuration parameters.
 */
export interface AppConfig {
  priceId: number;
  amount: number;
  currency: string;
}
